/**
 * Pending Work tools for the AI work queue.
 *
 * These tools enable AI agents to query and manage work items
 * that have been queued via webhooks.
 */

export { registerPendingWorkTools } from './tools.js';
